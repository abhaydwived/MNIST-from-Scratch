# MNIST-from-Scratch
 Implemented a fully connected neural network from scratch using only NumPy — no ML libraries. Trained on the MNIST dataset with 90%+ accuracy using forward and backward propagation, softmax activation, and cross-entropy loss. Demonstrates strong understanding of neural network internals and manual optimization.
